[build]
    functions = "serverless/"
[dev]
    targetPort = 3000
    autoLaunch = false
[functions]
    directory = "serverless/"
    node_bundler = "esbuild"
[[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
